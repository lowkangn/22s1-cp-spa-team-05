#include "ParentTManager.h"
#include <string>
#include <vector>

std::string ParentTManager::filter(QueryClause queryClause) {
	return std::string{ "0" };
}

void ParentTManager::add(Relationship relationship) {

}

void ParentTManager::clearDataBase() {
	//Clear data
}
