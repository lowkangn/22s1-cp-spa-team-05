#include "ParentManager.h"
#include <string>
#include <vector>

std::string ParentManager::filter(QueryClause queryClause) {
	return std::string{ "0" };
}

void ParentManager::add(Relationship relationship) {

}

void ParentManager::clearDataBase() {
	//Clear data
}
